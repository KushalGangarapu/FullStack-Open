{
  "name": "bloglist-monorepo",
  "version": "1.0.0",
  "description": "Part 7 BlogList Fullstack Application",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "build": "npm run build --prefix client",
    "start": "npm run start:prod --prefix server",
    "lint": "npm run lint --prefix server && npm run lint --prefix client",
    "test": "npm run test --prefix server && npm run test:run --prefix client"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
